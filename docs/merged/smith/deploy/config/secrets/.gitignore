# Live deployment secrets never enter the repo. Only the *.example template, this
# .gitignore, and the README are tracked. Everything else here (the real
# roles.env, the wake/webhook secret files written by provisioning) is ignored.
*
!.gitignore
!README.md
!*.example
