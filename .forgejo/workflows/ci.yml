name: dogfood-ci

on:
  pull_request:
    branches:
      - main

jobs:
  dev-check:
    runs-on: host
    steps:
      - name: checkout without external actions
        run: |
          set -eu
          mkdir -p "$GITHUB_WORKSPACE/repo"
          cd "$GITHUB_WORKSPACE/repo"
          git init
          git remote add origin "$GITHUB_SERVER_URL/$GITHUB_REPOSITORY.git"
          git -c http.extraheader="AUTHORIZATION: token $GITHUB_TOKEN" \
            fetch --depth=1 origin "$GITHUB_SHA"
          git checkout --detach FETCH_HEAD
      - name: cargo dev-check
        run: |
          set -eu
          cd "$GITHUB_WORKSPACE/repo"
          export CARGO_TARGET_DIR="$HOME/src/rust/harness/target/dogfood-ci"
          echo "TODO: cargo dev-check"
