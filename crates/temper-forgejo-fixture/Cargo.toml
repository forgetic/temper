[package]
name = "temper-forgejo-fixture"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Shared test fixture for local throwaway Forgejo servers and runners."

[dependencies]
reqwest = { workspace = true, features = ["blocking"] }
sha2 = "0.10"

[lints]
workspace = true
