[package]
name = "temper-config"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Schema-based TOML configuration + credentials for the temper deployment (config/credentials files, layered precedence, resolution into plain runtime settings)."

[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
toml = { workspace = true }
thiserror = { workspace = true }

[lints]
workspace = true
