[package]
name = "temper-interaction-service"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "Deployable REPL/HTTP interaction service and transport glue."

[dependencies]
serde.workspace = true
serde_json.workspace = true
temper-forge = { path = "../temper-forge" }
temper-forge-forgejo = { path = "../temper-forge-forgejo" }
temper-interaction = { path = "../temper-interaction" }
tokio = { version = "1", features = ["macros", "rt", "sync"] }

[dev-dependencies]
async-trait.workspace = true
temper-forge-memory = { path = "../temper-forge-memory" }

[lints]
workspace = true
