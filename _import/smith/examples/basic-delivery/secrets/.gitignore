# Ignore EVERYTHING in this directory (real keys, generated per-role tokens such
# as roles.env, sourced .env files) ...
*
# ... except the templates and this file itself, so the scaffold stays tracked
# while no live secret can ever be committed.
!.gitignore
!*.example
