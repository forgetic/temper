# Codebase-memory live scenario flow keeps Forgejo/runner/Temper setup,
# fake MCP wiring, and final-state probes together until manifest primitives are extracted.
crates/temper-testing/src/live_manifest/codebase_memory.rs

# Codebase-memory tool bridge carries allowlist, workspace scoping, and
# production observability at one registration boundary; split with next MCP bridge refactor.
crates/temper-agent/src/codebase_memory.rs

# Startup assignment recovery keeps staged prior-boot ownership, exact heartbeat
# reattachment, and protocol dispatch state together at the daemon core boundary.
crates/temper-worker-registry/src/daemon_core.rs

# Recovery protocol coverage shares the daemon-core fixtures needed to prove
# mismatched, repeated, and orphaned heartbeat behavior.
crates/temper-worker-registry/src/daemon_core_tests.rs

# Worker transport integration coverage shares one daemon harness for polling,
# attempt-fenced result acknowledgement, authentication, and capacity behavior.
crates/temper-engine/tests/transport.rs

# Forge result fixtures centralize durable assignment identities and seeded
# workflow state so attempt-aware apply tests exercise one consistent boundary.
crates/temper-engine/tests/forge_apply/support.rs

# Durable assignment claim, refresh, rollback, and quarantine all share one
# conditional Forge write boundary so recovery CAS semantics remain auditable.
crates/temper-workflow/src/lease/manager.rs

# Prepared containment runtime keeps spawn validation, exactly-once cleanup
# coordination, fail-closed retry, and observer delivery in one auditable state machine.
crates/temper-process-containment/src/runtime.rs

# The wake coordinator is one pure transition owner for bounded lane compaction,
# apply deferral, timer generations, global admission, and dirty completion semantics.
crates/temper-engine/src/daemon/wake_coordinator.rs

# The daemon machine owns the startup barrier alongside poll waiters and claim
# transitions; keeping these states together prevents dispatch bypass paths.
crates/temper-engine/src/daemon/machine.rs

# The daemon handle composes shared executor capabilities for wake execution,
# artifact context, and trace storage/query wiring at one construction boundary.
crates/temper-engine/src/daemon/handle.rs

# Startup recovery inventory and orphan convergence share the service boundary
# with daemon construction so no feed can bypass durable claim reconstruction.
crates/temper-engine-service/src/runtime.rs

# Feed enrichment and durable-assignment reconstruction use the same workflow
# compilation and Forge snapshot rules; keep them co-located for parity audits.
crates/temper-engine/src/feed/mod.rs

# Workspace recovery is one atomic preservation/replay/quarantine protocol;
# keeping its phases together makes destructive-operation ordering auditable.
crates/temper-worker/src/workspace/recovery.rs

# Git-backed recovery scenarios share one origin/checkout fixture and recovery
# assertions; keeping them together documents the end-to-end safety matrix.
crates/temper-worker/tests/workspace.rs

# Workspace git wrapper keeps checkout, diff probing, and cleanup helpers together;
# new diff-path observability reuses the same command boundary.
crates/temper-worker/src/workspace.rs

# Implementation-PR handoff live scenario flow keeps create/refresh orchestration,
# provider setup, and final-state probing together; fake LLM scripting is split out.
crates/temper-testing/src/live_manifest/handoff.rs

# Forge applier success path coordinates branch push, PR create/update, review
# handoff, and observable result application; split with the next applier refactor.
crates/temper-engine/src/forge_applier/success.rs

# temper-log emit is the central closed event projection table; keeping emit
# constructors together preserves event-schema reviewability until codegen lands.
crates/temper-log/src/emit/mod.rs

# Existing dense integration coverage; split when the Forgejo issue workflow
# fixtures get another focused refactor.
crates/temper-forge-forgejo/tests/issues.rs

# Argument parser matrix tests are table-like and mostly assertion fixtures.
crates/temper-testing/src/worker_bin/args_parse_tests.rs

# Shared scenario builder data for real Forgejo tests; splitting risks hiding
# the scenario flow until the fixture model is revised.
crates/temper-testing/src/scenarios.rs

# Workflow planner integration coverage with many case-specific assertions.
crates/temper-workflow/tests/planning.rs

# Multi-repository runner scenarios with coupled setup, scan, and assertion
# phases; should be split during the next multi-repo test cleanup.
crates/temper-runner/tests/multi_repo_filesystem.rs
crates/temper-runner/tests/multi_repo_worker.rs

# Workflow safety/property coverage remains scenario-dense but under the hard
# limit after the current focused splits.
crates/temper-workflow/tests/safety_properties.rs
crates/temper-workflow/tests/multi_artifact_effects.rs
crates/temper-workflow/tests/execution.rs

# Durable create-intent budget and crash-replay matrices share one ten-child DAG
# fixture so operation ceilings remain directly comparable across checkpoints.
crates/temper-workflow/tests/create_intent_recovery.rs

# Worker automation has many constructor-style helpers around one role flow.
crates/temper-runner/src/worker/automation.rs

# Reference-delivery validator keeps one operator-facing validation flow and
# its helper checks together for readable diagnostic sequencing.
crates/temper-reference-delivery-validator/src/reference_delivery_validator.rs

# Init flow tests keep prompt, local-write, and apply-path scenarios together so
# workflow artifact assertions share the same offline provisioner seam.
crates/temper-cli-init/tests/run_init.rs

# Memory/filesystem backend contract tests retain grouped backend behavior.
crates/temper-forge-memory/tests/backend.rs
crates/temper-forge-filesystem/tests/issues.rs

# Configuration surfaces intentionally remain façade modules while the schema
# and resolution layers carry deployment policy; split after the config model
# is next refactored.
crates/temper-cli-config/src/lib.rs
crates/temper-config/src/schema.rs
crates/temper-config/src/resolve.rs

# The canonical closed JSON Schema mirrors every on-disk section in one
# reviewable tree so runtime and operator schema changes cannot drift apart.
crates/temper-config/src/json_schema.rs

# Standalone assembly keeps engine, in-process worker, startup ordering, and
# operator readiness output at the single composition root.
crates/temper-cli-daemon/src/standalone/mod.rs

# Config resolution tests cover cross-section defaults and validation matrices;
# split when the next config test fixture cleanup lands.
crates/temper-config/src/tests.rs

# Reference log projection coverage shares one typed tracing capture layer so
# human and machine fields are asserted against the same emitted records.
crates/temper-log/tests/reference_projection.rs

# Agent trace capstones intentionally keep producer/process fixtures, worker
# spools, engine query/export, web, and OTel assertions in one leak-detection path.
crates/temper-worker/src/trace/full_path_tests.rs

# Attempt cleanup proof, cancellation escalation, and durable result admission
# remain in the worker's single pure transition owner so no alternate terminal path bypasses them.
crates/temper-worker/src/worker_machine.rs
crates/temper-worker/src/worker_machine/watchdog.rs

# The out-of-process run owner keeps process cleanup, endpoint joins, side-channel
# fencing, and result-file acceptance in one auditable quiescence sequence.
crates/temper-worker/src/out_of_process_runner/managed_run.rs

# The worker shell keeps attempt controls, cleanup observers, task lifetime, and
# unified completion publication at the one imperative job-dispatch boundary.
crates/temper-worker/src/worker_shell.rs
