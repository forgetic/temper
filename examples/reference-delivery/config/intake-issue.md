Operators running several deployments of this service can't tell which
environment a running process belongs to.

Make the startup banner show a greeting we can set per environment, so an
operator can tell deployments apart at a glance.

That's the whole ask — I haven't worked out the details.
