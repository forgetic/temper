# Ignore EVERYTHING in this directory (generated per-role tokens such as
# roles.env, the webhook secret file, sourced .env files) ...
*
# ... except the templates and this file itself, so the scaffold stays tracked
# while no live secret can ever be committed.
!.gitignore
!*.example
