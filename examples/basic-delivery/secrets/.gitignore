# Ignore EVERYTHING in this directory (the generated per-role/bot
# credentials.toml, the webhook secret file, sourced .env files) ...
*
# ... except the templates and this file itself, so the scaffold stays tracked
# while no live secret can ever be committed.
!.gitignore
!*.example
