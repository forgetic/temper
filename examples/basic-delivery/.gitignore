# Runtime artifacts the fixed launcher creates. The tracked scaffold is config/,
# observability.md, run.sh, README.md, and secrets/.gitignore (to keep any old
# local secrets ignored).
/run/
/logs/
*.pid
*.log
