Build a small two-step product change through the plan-centric dogfood workflow.

This source feature intentionally arrives with repository-default branch metadata, reproducing the production-shaped input that must not select implementation topology. The architect should omit an engine-derivable target branch. Temper must derive a non-default feature branch, carry it through decomposition and a tester-requested implementation follow-up, then land one aggregate feature PR to main.

<!-- temper:workflow
{"kind":"feature","target_branch":"main"}
-->
