Build a small two-step product change through the plan-centric dogfood workflow.

The architect should create a plan issue with a feature branch, decompose it into sequential code children, validate the feature branch, and land the aggregate feature PR to main.
