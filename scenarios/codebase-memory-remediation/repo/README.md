# Demo repository

Seed fixture for the live codebase-memory agent scenario. `src/lib.rs` deliberately routes alias retries using the raw topic after the first attempt. The engineer must use the graph-selected implementation and test, verify them through ordinary repository tools after the controlled provider failure, and repair the behavior.
