Repair alias retry worker affinity with the smallest semantic change.

First use a targeted current-root result and caller trace, then exercise the
selected unavailable source. Follow its fixed safe guidance with conventional
discovery and do not immediately retry codebase-memory.

After fallback, retain both useful independent roots. Consume the implementation
refinement, caller trace, deliberate duplicate refinement, implementation source,
and focused behavioral-test source in later turns. Once that chain is complete,
attempt the requested broad, duplicate, and post-decision graph reads so the
generic local convergence boundary is observable. Do not infer provider values
or bypass a local denial.

Use conventional source reading after convergence, write only the selected
one-file repair, and validate with `cargo fmt --check && cargo test --quiet`.
