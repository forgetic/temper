Repair the selected retry behavior with the smallest semantic change.

In the first model turn, request one targeted graph discovery and then one shell
process barrier. Treat the successful graph result as the decision anchor even
when the later barrier is denied locally. In later turns consume the selected
implementation refinement, caller/model trace, typed current-root caller/model
source, and typed focused behavioral-test source before mutation.

After the complete evidence chain, read the selected source conventionally,
apply only the minimal repair, and run the focused host validation. If the
process barrier executed or the graph anchor cannot be consumed safely, stop
without a product.
