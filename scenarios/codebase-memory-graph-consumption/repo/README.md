# Demo repository

Seed fixture for the live graph-consumption scenario. `src/lib.rs` deliberately
routes alias retries using the raw topic after the first attempt. The engineer
must consume the declared graph, refinement, trace, implementation-source, and
focused-test-source chain from the confirmed current root before repairing the
behavior.
