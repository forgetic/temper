Validate bounded codebase-memory graph-consumption evidence on the live manifest runner.

Alias retries must remain on the original ordered worker. First confirm the provider's normalized identity is ready for this active checkout root. Then make exactly this same-work-item chain before editing:

1. targeted graph search for `alias retry worker affinity`;
2. code refinement for `retry_worker_topic`;
3. trace `retry_worker_topic` to its caller;
4. read `src/lib.rs` through the safe codebase-memory source tool; and
5. read `tests/retry_affinity.rs` through that same confirmed binding.

Do not use broad architecture or inventory calls, and do not make additional codebase-memory calls. Repair `src/lib.rs` so every retry uses `canonical_topic` when it is present, preserve non-aliased behavior, and validate with `cargo fmt --check && cargo test --quiet`.
