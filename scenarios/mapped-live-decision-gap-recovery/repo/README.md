# Mapped live decision-gap recovery fixture

This minimal crate starts with a retry-affinity defect behind a public caller,
private implementation, and focused behavioral regression. The temporary live
provider mints the implementation/caller/test chain at runtime.
