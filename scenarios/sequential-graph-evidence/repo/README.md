# Demo repository

Seed fixture for the sequential graph-evidence scenario. The provider declares
each dependent target only after its preceding successful current-root result;
the test target is exposed only after the implementation source has been read.
