Validate the codebase-memory agent integration on the live manifest runner.

The engineer should receive codebase-memory tool configuration through real Temper, call the fake codebase-memory search tool before editing, and write `MEMORY_NOTES.md` from the fake MCP result.
