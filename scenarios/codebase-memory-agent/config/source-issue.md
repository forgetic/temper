Validate the codebase-memory agent integration without a real provider or real codebase-memory binary.

The engineer should receive enabled tool configuration, call the fake codebase-memory search tool, and write a deterministic note influenced by the fake MCP result.
