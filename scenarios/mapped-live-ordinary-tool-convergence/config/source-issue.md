Repair the selected retry behavior with the smallest semantic change.

First consume the successful targeted graph chain in later turns: discovery,
refinement, caller trace, current-root implementation, and current-root focused
test. Observe the expected graph-local exploration closure without retrying a
graph tool.

Then follow the deterministic ordinary-tool recovery sequence. Preserve
canonical tool identities, correct malformed or ambiguous arguments rather than
repeating them, and change an identical non-retryable invocation after the
local redirect. Produce only the minimal selected repair. Validate formatting
and tests, then submit through the host gate.
